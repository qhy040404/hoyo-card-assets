name: CI
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger update
        run: curl -GLi --data-urlencode 'job=Update Hoyo-Card-Assets&token=${{ secrets.BUILD_TOKEN }}&cause=git push' https://jenkins.qhy04.com/buildByToken/build
